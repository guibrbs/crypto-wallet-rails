<h2 class="site-title"><span class="site-title yellow">CRYPTO</span>WALLET</h2>
<div class="welcome-container">
    <img class="coin-illustration" src="/assets/Bitcoin-amico.svg">
    <div class="container-wrapper">
        <h1 class="container-title"><%= t('welcome_title')%></h1>
        <p class="description"><%= t('welcome_description')%></p>
        <%= link_to "#{MiningType.model_name.human}", mining_types_path, { :class => "btn btn-primary btn-lg"} %>
        <%= link_to "#{Coin.model_name.human}", coins_path, { :class => "btn btn-primary btn-lg"} %>
    </div>
</div>
